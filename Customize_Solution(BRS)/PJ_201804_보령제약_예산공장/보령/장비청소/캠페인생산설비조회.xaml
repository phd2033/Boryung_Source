<ShopFloorUI:ShopFloorCustomWindow 
    x:Class="보령.캠페인생산설비조회"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit" 
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    xmlns:c1="http://schemas.componentone.com/winfx/2006/xaml"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:icommon="clr-namespace:LGCNS.iPharmMES.Common;assembly=LGCNS.iPharmMES.Common"    
    xmlns:icontrol="clr-namespace:LGCNS.iPharmMES.Controls;assembly=LGCNS.iPharmMES.Controls"
    xmlns:ShopFloorUI="clr-namespace:ShopFloorUI;assembly=ShopFloorUI.Common"
    xmlns:local="clr-namespace:보령"
    x:Name="Main" Width="600" Height="350" Title="캠페인생산설비조회">
    <ShopFloorUI:ShopFloorCustomWindow.Resources>
        <local:캠페인생산설비조회ViewModel x:Key="VM"/>
        <Style x:Key="HeaderStyle" TargetType="c1:DataGridColumnHeaderPresenter">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </ShopFloorUI:ShopFloorCustomWindow.Resources>
    <toolkit:BusyIndicator IsBusy="{Binding IsBusy}" DataContext="{Binding Source={StaticResource VM}}">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Loaded">
                <i:InvokeCommandAction Command="{Binding LoadedCommand}" CommandParameter="{Binding ElementName=Main}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <Grid x:Name="LayoutRoot" Margin="2" >
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" x:Name="HeaderSection">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <sdk:Label Grid.Column="0" Content="캠페인 생산 여부" HorizontalAlignment="Center" FontSize="15" FontWeight="Bold" Margin="159,0,20,0" Width="113"/>
                <ComboBox Name="campaignYN" Grid.Column="1" FontSize="15" FontWeight="Bold" ItemsSource="{Binding YnOptions}" SelectedItem="{Binding Path = SelectedYnValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10,7,142,8"/>
            </Grid>

            <Grid Grid.Row="2" x:Name="MainSection">
                <c1:C1DataGrid  Grid.Column="0" x:Name="dgProductionOutput"
                       GridLinesVisibility="All" AutoGenerateColumns="False" HeadersVisibility="Column" HorizontalScrollBarVisibility="Auto"
                       CanUserAddRows="False" CanUserEditRows="False" CanUserRemoveRows="False" CanUserReorderColumns="False"
                       AlternatingRowBackground="White" RowBackground="White"
                       ItemsSource="{Binding CampaignList}" Grid.ColumnSpan="2">
                    <c1:C1DataGrid.Columns>
                        <c1:DataGridTextColumn Header="장비번호"  Binding="{Binding Path=EQPTID}" HorizontalAlignment="Center" HeaderStyle="{StaticResource HeaderStyle}"/>
                        <c1:DataGridTextColumn Header="캠페인 생산 여부"  Binding="{Binding Path=CAMPAIGNYN}" HorizontalAlignment="Center" HeaderStyle="{StaticResource HeaderStyle}"/>
                        <c1:DataGridTextColumn Header="캠페인 횟수" Binding="{Binding Path=CAMPAIGNCNT}" HorizontalAlignment="Center" HeaderStyle="{StaticResource HeaderStyle}"/>
                        <c1:DataGridTextColumn Header="캠페인 생산일수" Binding="{Binding Path=CAMPAIGNPERIOD}" HorizontalAlignment="Center" HeaderStyle="{StaticResource HeaderStyle}"/>
                    </c1:C1DataGrid.Columns>
                </c1:C1DataGrid>
            </Grid>

            <Grid Grid.Row="3" x:Name="Footer">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Button x:Name="OKButton" Content="{Binding W[CUIDataWriteToEBRButtonText], Source={StaticResource VM}, FallbackValue='기록'}" Command="{Binding Path=ConfirmCommand}" Width="120" Height="35" Margin="10,0"/>
                    <Button x:Name="CancelButton" Content="{Binding W[CUICloseButtonText], Source={StaticResource VM}, FallbackValue='취소'}" Click="CancelButton_Click" Width="120" Height="35" Margin="10,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </toolkit:BusyIndicator>
</ShopFloorUI:ShopFloorCustomWindow>