<ShopFloorUI:ShopFloorCustomWindow x:Class="보령.설비액션목록"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ShopFloorUI="clr-namespace:ShopFloorUI;assembly=ShopFloorUI.Common"
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
    xmlns:c1="http://schemas.componentone.com/winfx/2006/xaml"
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:local="clr-namespace:보령"
    mc:Ignorable="d" Title="설비액션목록"
    Name="Main" Width="600" Height="550">
    <ShopFloorUI:ShopFloorCustomWindow.Resources>
        <local:설비액션목록ViewModel x:Key="ViewModel"/>
        <Style TargetType="c1:DataGridColumnHeaderPresenter">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </ShopFloorUI:ShopFloorCustomWindow.Resources>
    <Grid x:Name="LayoutRoot" Background="White" DataContext="{Binding Source={StaticResource ViewModel}}">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Loaded">
                <i:InvokeCommandAction Command="{Binding LoadedCommandAsync}" CommandParameter="{Binding ElementName=Main}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <toolkit:BusyIndicator Name="BusyIndicator" IsBusy="{Binding IsBusy}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Name="btnGroup1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <sdk:Label Grid.Column="0" Width="100" Height="40" Content="설비코드" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15" FontWeight="Bold"/>
                    <TextBox Name="txtSelEqptId" Grid.Column="1" KeyDown="txtSelEqptId_KeyDown" Margin="5" Padding="10" VerticalAlignment="Stretch" VerticalContentAlignment="Center"/>
                    <Button Name="btnSelectEqpt" Grid.Column="2" Content="입력" Width="100" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center" Click="btnSelectEqpt_Click"/>
                </Grid>
                <sdk:Label Grid.Row="1" Content="대상 설비 목록" Margin="10,0,10,0"  HorizontalAlignment="Left" VerticalAlignment="Bottom" FontSize="15" FontWeight="Bold" />

                <!--<Rectangle Fill="Transparent" Grid.Row="2" Margin="0,0,10,0" Canvas.ZIndex="2"/>-->
                <ScrollViewer x:Name="scrollbar" Grid.Row="2" Height="Auto" Width="Auto">
                    <c1:C1DataGrid x:Name="GridRequestOutList" HeadersVisibility="Column" SelectionMode="SingleRow"
                               AutoGenerateColumns="False" CanUserAddRows="False" CanUserEditRows="False" CanUserRemoveRows="True" CanUserResizeRows="False" 
                               ScrollMode="RealTime" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
                               ItemsSource="{Binding ListRequestOut}" >
                        <c1:C1DataGrid.Columns>
                            <c1:DataGridTextColumn Header="설비코드" Binding="{Binding Path=EQPTID}" Width="100"/>
                            <c1:DataGridTextColumn Header="설비이름" Binding="{Binding Path=EQPTNAME}" Width="*"/>
                            <c1:DataGridTextColumn Header="설비군" Binding="{Binding Path=EQCLNAME}" Width="160"/>
                        </c1:C1DataGrid.Columns>
                    </c1:C1DataGrid>
                </ScrollViewer>
                <Grid Grid.Row="3" Name="btnGroup2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Width="90" Height="35" HorizontalAlignment="Right" VerticalAlignment="Center" Content="Delete" Name="btnDelete" Margin="0,10,50,5" Command="{Binding RemoveEqptCommandAsync}"/>
                    <Button Grid.Column="1" Width="90" Height="35" HorizontalAlignment="Right" VerticalAlignment="Center" Content="다음" Name="btndelete" Margin="0,10,172,5" Command="{Binding ConfirmCommandAsync}" Grid.ColumnSpan="2"/>
                    <Button Grid.Column="2" Width="90" Height="35" HorizontalAlignment="Left" VerticalAlignment="Center" Content="취소" Name="btnCancel" Margin="50,10,0,5" Click="btnCancel_Click" RenderTransformOrigin="0.469,1.019"/>
                </Grid>
            </Grid>
        </toolkit:BusyIndicator>
    </Grid>
</ShopFloorUI:ShopFloorCustomWindow>
